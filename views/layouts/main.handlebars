<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{appName}}</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <div class="logo">
            <img src="/images/logo.png" alt="MicroBlog Logo">
            <h1>{{appName}}</h1>
        </div>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                {{#if loggedIn}}
                <li><a href="/profile">Profile</a></li>
                <li><a href="/logout">Logout {{userId}}</a></li>
                {{else}}
                <li><a href="/login">Login/Register</a></li>
                {{/if}}
            </ul>
        </nav>
    </header>
    <div class="content-container">
        {{{body}}}
    </div>
    <footer>
        <p>&copy; {{copyrightYear}} {{appName}}. All rights reserved.</p>
    </footer>
</body>
</html>
